//in JS we can store different type in an array

let selectedColors = ['red', 'blue'];
console.log(selectedColors[0]);

//adding element to running array
let selectedColorsOne = ['red', 'blue'];
selectedColorsOne[2] = 'purple'
console.log(selectedColorsOne);
