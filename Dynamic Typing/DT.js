//there are 2 types of languages
// Static Lang
    //Once variable's value is set it cannot be changed in the future.

//Dynamic Lang
    //the type and variable can be changed at runtime

    let name = 'Rahul'; //string
    let age = 10; //number
    let isApproved = true; //boolean
    let fName = undefined;
    let lName = null;

    console.log(typeof name) 
    name = 1
    console.log(typeof name)